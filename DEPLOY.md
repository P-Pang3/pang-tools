# 배포와 업데이트

## 폴더 구조

개발 소스는 한 곳에 둔다. `core/` 를 프로그램마다 복사해두면 고칠 때마다
세 곳을 고쳐야 하고, 언젠가 한 곳을 빠뜨린다. 대신 `build.py` 가 각
프로그램에 **실제로 필요한 모듈만** 골라 담아 독립 폴더를 만든다.

```
개발 (이 저장소)                배포본 (사용자가 받는 것)
  main.py                        트릭스터 줍기 매크로/
  engine.py                        줍기 매크로 실행.bat
  autoclick.py                     app/          ← 업데이트가 갈아끼움
  core/                              main.py
  build.py                           core/...
  release.py                         version.json
                                   data/         ← 사용자 것. 안 건드림
                                   읽어보세요.txt
```

**`app/` 과 `data/` 를 나눈 것이 자동 업데이트의 전제다.** 이게 없으면
업데이트할 때마다 사용자가 만든 템플릿과 설정이 날아간다.

## 프로그램별 크기

| 프로그램 | core 모듈 | 배포본 | 업데이트 zip | 필요 패키지 |
|---|---|---|---|---|
| 줍기 | 20개 | 219 KB | 72 KB | pynput, opencv, mss, numpy, Pillow |
| 사냥 | 20개 | 219 KB | 72 KB | 위와 같음 |
| 오토클릭 | 8개 | 79 KB | 28 KB | **pynput 뿐** |

오토클릭은 화면을 보지 않으므로 OpenCV 가 필요 없다. 설치가 훨씬 가볍다.

## 처음 한 번 — 저장소 만들기

```bash
gh auth login                       # 한 번만
git init
git add .
git commit -m "첫 커밋"
gh repo create trickster-macro --public --source=. --push
```

`--public` 이어야 사용자가 인증 없이 업데이트를 받을 수 있다.

## 릴리스 (업데이트 배포)

```bash
python release.py 1.1.0 -m "이동 속도 조정"
```

한 줄이면 끝난다. 하는 일:

1. `core/version.py` 의 `VERSION` 을 올린다
2. 세 프로그램 배포본을 만든다
3. `gh` 로 태그 `v1.1.0` 릴리스를 만들고 zip 6개를 올린다

**처음 릴리스할 때만** 저장소를 알려준다 (그 뒤로는 기억한다):

```bash
python release.py 1.0.0 --owner 내계정 --repo trickster-macro -m "첫 배포"
```

올리기 전에 확인만 하려면 `--dry-run` 을 붙인다. 버전도 되돌려준다.

## 사용자가 겪는 흐름

1. `트릭스터 줍기 매크로.zip`(= `pickup-full.zip`) 을 받아 아무 데나 푼다
2. `줍기 매크로 실행.bat` 을 두 번 누른다 — 처음 한 번만 패키지를 자동 설치
3. 다음부터는 켜면 바로 실행
4. 새 버전이 올라오면 **켤 때 위쪽에 띠가 뜬다** → `업데이트` 누르면
   받아서 적용하고 스스로 다시 시작한다. 설정과 템플릿은 그대로 남는다

## 업데이트가 동작하는 방식

- 시작할 때 백그라운드로 `api.github.com/repos/{owner}/{repo}/releases/latest`
  를 한 번 본다. 네트워크가 없으면 조용히 넘어간다
- 새 버전이면 알림 띠를 띄운다. **없으면 화면에 아무것도 안 생긴다**
- 받는 것은 `app/` 만 담긴 zip (72 KB). `data/` 는 건드리지 않는다
- 실행 중인 폴더는 스스로 지울 수 없으므로, 작은 배치를 만들어 넘기고
  프로그램은 종료한다. 배치가 종료를 기다렸다가 폴더를 바꾸고 다시 띄운다
- 교체에 실패하면 이전 `app/` 을 되돌린다

## 주의

- **저장소는 Public 이어야 한다.** Private 이면 배포본에 토큰을 넣어야 하는데
  그 순간 토큰이 유출된다
- GitHub API 는 인증 없이 시간당 60회. 시작할 때 한 번만 부르므로 넉넉하다
- 매크로 + 자동 다운로드 조합은 백신이 의심할 수 있다. 소스 배포라
  exe 보다는 덜하지만, 사용자가 경고를 볼 수 있다는 점은 알아두자
- `data/` 는 `.gitignore` 에 있다. 설정에는 좌표와 개인 취향이 들어 있어
  남에게 쓸모가 없고, 올리면 사용자 것을 덮을 위험도 있다
